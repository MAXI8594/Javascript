/**
 * 1.Imprima las tablas de multiplicar del 2 al 12 utilizando la declaración for.

Pista: anidamiento.
 */

for(let i = 1 ; i < 13; i ++){
var num = i;
for(let i = 1 ; i < 13; i ++){
    var multp = i * num;
    var cont = i ; 
    console.log([num] + '*' + [cont] + ' =' + multp);
}
}

/**
 * 2.Realice el equivalente con la declaración while y do-while
 */

